start a virtual environment  
`python -m venv venv`  
activate the environment  
`venv\Scripts\activate`  
install necessary packages  
`pip install -r requirements.txt`  
copy .envdemo, rename it to .env and enter your creds

run app.py  
`python app.py`  
When prompted in the console, choose which program you want made into a textbook.
